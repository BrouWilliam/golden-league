let swiperPortfolio = new Swiper(".containerzada", {
    cssMode: false,
    loop: true,

    navigation: {
      nextEl: ".swiper-pagination",
      prevEl: ".swiper-pagination",
    },
    pagination: {
      el: ".swiper-pagination",
    },
});